value
error
values
errors
pristine
onChange(value, name, stayPristine) - eval pristine, set value
check() - eval pristine

  <Validate validators={[notEmpty, email]}>
    {({value, errors, pristine}) => (
      <input type="text"
        value={value}
        onChange={doSomething} />
    )}
  </Validate>

  <Validate>
    {({check, group}) => {
      <div>
        <Validate parent={group}></Validate>
        <Validate parent={group}></Validate>
        <button onClick={check() && submit()}>
          Submit
        </button>
      </div>
    }}
  </Validate>


Validate
  Validate onChange(val, name1)
  Validate onChange(val, name2)


errors = {
  error: []
  errors: {
    field: {
      error: [],
      errors: {...}
    }
  }
}

values = {
  value: 5,
  values: {
    field: {
      value: 44
      values: {...}
    }
  }
}
